package com.developance.data.paging

import com.developance.model.data.defaultRandomTopic

class TopicsPagingSourceTest {
    private val mockTopics = listOf(
        defaultRandomTopic,
        defaultRandomTopic,
        defaultRandomTopic
    )
}